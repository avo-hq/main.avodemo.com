# Authorizes the HttpUser HTTP resource (avo-http_resource). Its model class is the
# dynamically-defined Avo::HttpResource::HttpUser, so Pundit resolves the policy here.
class Avo::HttpResource::HttpUserPolicy < BaseAvoPolicy
end
